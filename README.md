# Платформа сбора работ на конкурс
## что тут есть кароч
- [Требования](#требования)
- [Установка](#установка)
- [Настройка окружения](#настройка-окружения)
- [Настройка S3 (Beget)](#настройка-s3-beget)
- [Очереди](#очереди)
- [Тестовые пользователи](#тестовые-пользователи)
- [Запуск проекта](#запуск-проекта)
- [API endpoints](#api-endpoints)
- [Структура проекта](#структура-проекта)
- [Проверка работоспособности](#проверка-работоспособности)

## я требую вот это

- PHP 8.1+
- Composer
- MySQL 5.7+ / MariaDB 10.2+
- OpenServer / XAMPP / Laravel Herd
- S3-совместимое хранилище (Beget / MinIO / Amazon S3)

## Установка

```bash
# 1. Клонировать репозиторий
git clone <url-репозитория>
cd contest-platform

# 2. Установить зависимости
composer install

# 3. Скопировать файл окружения
copy .env.example .env

# 4. Сгенерировать ключ приложения
php artisan key:generate
```

## Настройка окружения

### База данных (`.env`)
```env
DB_CONNECTION=mysql
DB_HOST=MySQL-8.0
DB_PORT=3306
DB_DATABASE=contest_platform
DB_USERNAME=root
DB_PASSWORD=
```

### Очереди (использую database)
```env
QUEUE_CONNECTION=database
```

### S3-хранилище
```env
FILESYSTEM_DISK=s3
AWS_ACCESS_KEY_ID=ваш_access_key
AWS_SECRET_ACCESS_KEY=ваш_secret_key
AWS_DEFAULT_REGION=ru1
AWS_BUCKET=ваш_бакет
AWS_ENDPOINT=https://s3.ru1.storage.beget.cloud
AWS_URL=https://s3.ru1.storage.beget.cloud/ваш_бакет
AWS_USE_PATH_STYLE_ENDPOINT=true
AWS_THROW=true
```

## Настройка S3

1. Зарегистрируйтесь на Beget и получите доступ к S3-хранилищу
2. Создайте бакет (bucket)
3. Получите ключи доступа (Access Key и Secret Key)
4. Укажите данные в `.env` как показано выше

## Очереди

Проект использует очередь:

```bash
# Создать таблицу для очередей
php artisan queue:table
php artisan migrate

# Запустить воркер
php artisan queue:work
```

## Тестовые пользователи

После запуска миграций с сидами создаются пользователи:

| Роль | Email | Пароль |
|------|-------|--------|
| Администратор | admin@example.com | admin123123 |
| Жюри | jury@example.com | jury123123123 |
| Участник | participant@example.com | participant123123 |

## 🏃 Запуск проекта

```bash
# 1. Запустить миграции и сиды
php artisan migrate --seed

# 2. Очистить кэш
php artisan optimize:clear

# 3. Запустить воркер очередей (в отдельном окне)
php artisan queue:work

# 4. Запустить сервер
php artisan serve
```

После запуска сайт доступен по адресу: `http://contest-platform` или `http://localhost:8000`

### Публичные
- `GET /` - главная страница
- `GET /login/{role}` - быстрый вход под ролью (для теста)

### Требуют авторизации
- `GET /dashboard` - личный кабинет
- `GET /submissions/create` - создание заявки
- `POST /submissions` - сохранение заявки
- `GET /submissions/{id}` - просмотр заявки
- `POST /submissions/{id}/attachments` - загрузка файла
- `POST /submissions/{id}/submit` - отправка на проверку
- `POST /submissions/{id}/status` - смена статуса (жюри)
- `POST /submissions/{id}/comments` - добавление комментария
- `GET /attachments/{id}/download` - скачивание файла

### Админка
- `GET /admin/contests` - управление конкурсами
- `GET /admin/users` - управление пользователями

## Структура проекта

```
app/
├── Console/           # Консольные команды
├── Exceptions/        # Обработка ошибок
├── Http/
│   ├── Controllers/   # Контроллеры
│   │   └── Web/       # Веб-контроллеры
│   ├── Requests/      # Form Requests (валидация)
│   └── Middleware/    # Middleware
├── Jobs/              # Очереди (ScanAttachmentJob, NotifyStatusChangedJob)
├── Models/            # Модели (Contest, Submission, Attachment, etc.)
├── Policies/          # Политики доступа
├── Providers/         # Сервис-провайдеры
└── Services/          # Сервисный слой (SubmissionService, AttachmentService)

resources/views/       # Blade шаблоны
├── layouts/           # Основной шаблон
├── participant/       # Страницы участника
├── jury/              # Страницы жюри
└── admin/             # Страницы администратора

database/
├── migrations/        # Миграции БД
└── seeders/           # Сиды с тестовыми данными
```

## Проверка работоспособности

### 1. Проверка очередей
```bash
php artisan queue:work
```

### 2. Проверка S3
```bash
php artisan tinker
>>> Storage::disk('s3')->put('test.txt', 'Hello')
>>> Storage::disk('s3')->exists('test.txt')
```

### 3. Проверка уведомлений
```bash
php artisan tinker
>>> App\Models\Notification::all()
```

### 4. Проверка статусов заявок
```bash
php artisan tinker
>>> App\Models\Submission::with('attachments')->get()
```

## Функционал по ролям

### Участник
- Создание заявок (draft)
- Загрузка файлов (до 3 шт, до 10MB, pdf/zip/png/jpg)
- Редактирование в статусах draft/needs_fix
- Отправка на проверку (submitted)
- Просмотр своих заявок
- Добавление комментариев
- Получение уведомлений о смене статуса

### Жюри
- Просмотр всех заявок
- Смена статусов (needs_fix/accepted/rejected)
- Добавление комментариев
- Скачивание файлов

### Администратор
- Управление конкурсами (создание, редактирование)
- Управление пользователями и их ролями

## Бизнес-логика

**Статусы заявок:**
- `draft` → `submitted` → (`accepted` | `rejected` | `needs_fix`)

**Правила:**
- Редактирование только в статусах `draft` и `needs_fix`
- Для отправки нужен минимум 1 файл со статусом `scanned`
- Максимум 3 файла на заявку
- Максимальный размер файла: 10MB
- Разрешённые типы: pdf, zip, png, jpg